# Data-Cleaning-task
Scaling Explanation:
I would prefer Min-Max Scaling when the data does not follow a normal distribution or when I need values strictly between 0 and 1. I would prefer Standardization when the data follows a Gaussian distribution or when the dataset has outliers, as it is more robust and centers the data around a mean of 0.
